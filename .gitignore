# Secrets & tokens — nooit committen
.env
*.json.bak
*token*.json
gsc_token.json
client_secret*.json

# Runtime-data
data/

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/
dist/
build/
*.log

# OS
.DS_Store

code_export.txt
STATE_export.txt
.env

# backups van .env nooit tracken
.env.save
*.save
data/poc/attachments/
scratch/

# Testrun-artefacten — kwamen bij elke pytest-run terug in de working tree
:mem:
notifications.json
village-core.zip
_to_delete/
*.patch

# Runtime-cache op de server (pip/HF e.d.). Puur voor de netheid — de deploy-guard
# negeert untracked bestanden sowieso (scripts/deploy.sh, --untracked-files=no).
.cache/
espanol/
output/
materiaal_memo.json
materiaal_memo.json.lock

# Archieven horen niet in de geschiedenis. Ze zijn groot, ze verouderen binnen een week, en git
# bewaart ze voor altijd — ook nadat je ze verwijdert. Ruim 40 MB van deze repo is zo binnengekomen
# (nooch-snap-22jul.tgz, backups/_cloud_export.tgz, _to_delete/nvhead.tar en tien kleinere).
# Een backup hoort naast de repo te staan, niet erin.
*.tgz
*.tar
backups/
